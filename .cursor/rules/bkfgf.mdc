---
alwaysApply: true
---

# 养娃管家后端开发规范（NestJS）

> 适用项目：`养娃管家` 微信小程序后端  
> 目标：在保证交付速度的同时，建立可维护、可扩展、可审计、可观测的工程体系。  
> 基线原则：**业务正确性优先、接口稳定性优先、数据安全优先、可观测性内建**。

---

## 1. 规范目标与适用范围

- 本规范覆盖后端开发全流程：设计、编码、测试、发布、运维。
- 以 NestJS + TypeScript 为唯一后端主栈，避免多语言带来的维护成本。
- 适用于 PRD v1.0 全模块：`dashboard`、`feeding`、`outfit`、`nearby`、`schedule`、`health`、`expense`、`voice`。
- 所有新增模块必须遵循本规范，不允许“临时脚本式”实现进入主干。

---

## 2. 技术栈与版本基线

- 运行时：`Node.js 20 LTS`
- 语言：`TypeScript`（严格模式）
- 框架：`NestJS 10.x`
- ORM：`TypeORM 0.3.x`
- 数据库：`MySQL 8.0+`
- 缓存：`Redis 7.x`（推荐，至少用于热点与幂等）
- 队列：`BullMQ`（提醒通知、异步语音任务、报表导出）
- 文档：`Swagger/OpenAPI`
- 测试：`Jest`（unit/integration/e2e）
- 日志：结构化 JSON（`pino` 或 Nest Logger 扩展）
- 部署：容器化（Docker），按环境配置（dev/staging/prod）

---

## 3. 架构设计原则

- 采用“**模块化单体优先**”架构：先做清晰边界，再决定是否拆微服务。
- 业务分层固定：`Controller -> Application Service -> Domain Logic -> Repository`。
- Controller 不写业务规则，仅做参数接收、鉴权、调用应用层。
- 领域规则必须内聚到模块服务，不允许散落在 Controller/拦截器中。
- 外部依赖（天气、地图、语音 ASR/NLU）统一走 `infrastructure/adapters`，禁止直接耦合第三方 SDK 于业务层。
- 业务事件（如提醒创建、预算预警）优先使用领域事件机制，避免强耦合同步调用链。

---

## 4. 目录与命名规范

```text
src/
  common/                 # 通用能力（异常、响应、装饰器、守卫、拦截器）
  config/                 # 配置与环境变量校验
  modules/
    dashboard/
    feeding/
    outfit/
    nearby/
    schedule/
    health/
    expense/
    voice/
    user/
    family/
  infrastructure/         # 三方集成（weather/map/asr/nlu/wechat）
  jobs/                   # 队列消费者与定时任务
  main.ts
```

- 文件命名统一 `kebab-case`，类名 `PascalCase`，变量/函数 `camelCase`。
- DTO 命名统一：`xxx.request.dto.ts`、`xxx.response.dto.ts`。
- 每个模块必须包含：`module`、`controller`、`service`、`repository`、`dto`、`entities`、`tests`。

---

## 5. TypeScript 与 NestJS 编码规范

- 必须开启 `strict: true`，禁止 `any`（除框架边界处明确标注并封装）。
- 所有公共方法必须声明返回类型。
- DTO 必须使用 `class-validator` + `class-transformer`。
- 统一启用全局 `ValidationPipe`：`whitelist=true`、`forbidNonWhitelisted=true`、`transform=true`。
- 禁止在业务代码中硬编码配置值，统一读取 `ConfigService`。
- 单文件行数建议 < 400 行；超过必须拆分。
- 复杂业务规则需增加简短注释，解释“为什么”，不解释“是什么”。

---

## 6. API 设计规范

### 6.1 路由与语义

- 采用 REST 风格，资源名复数，动作语义清晰。
- 与 PRD 对齐的核心路由基线：
  - `GET /dashboard/today`
  - `GET /feeding/recommendations`
  - `GET /outfit/recommendations`
  - `GET /nearby/places`
  - `GET /nearby/schools`
  - `POST /schedule/events`
  - `POST /health/records`
  - `POST /expenses`
  - `GET /expenses/summary`
  - `POST /voice/parse`
  - `POST /voice/execute`

### 6.2 响应结构统一

```json
{
  "code": 0,
  "message": "ok",
  "data": {},
  "requestId": "trace-xxx"
}
```

- `code=0` 表示成功，非 0 为业务错误。
- HTTP 状态码表达技术语义，`code` 表达业务语义。
- 所有接口必须返回 `requestId`，用于问题追踪。

### 6.3 分页与排序

- 分页参数统一：`page`、`pageSize`、`sortBy`、`sortOrder`。
- 默认 `page=1`、`pageSize=20`、`pageSize` 上限 100。
- 分页响应统一包含：`list`、`total`、`page`、`pageSize`。

### 6.4 幂等与防重

- `POST` 写操作支持 `Idempotency-Key`（尤其是记账、提醒创建、语音执行）。
- 同一用户 + 同一 Key + 时间窗内请求必须返回同一结果。
- 高风险操作（删除/批量修改）必须二次确认令牌。

---

## 7. 认证、授权与多成员隔离

- 小程序登录采用 `code2session`，后端签发 `JWT`（短期 Access + 可选 Refresh）。
- 权限模型：`user -> family -> memberRole`，所有业务数据必须带 `familyId` 作用域。
- 数据访问默认加 `familyId` 条件，防止越权读取。
- 敏感接口（导出、语音执行写操作、上传文件）必须鉴权 + 审计日志。
- 订阅消息发送遵循频控规则：同事件同用户单日上限与去重策略。

---

## 8. 数据库与持久化规范

- 表命名 `snake_case`，主键统一 `bigint` 或 `uuid`（全项目一致）。
- 通用字段强制：`id`、`created_at`、`updated_at`、`deleted_at`、`created_by`、`updated_by`。
- 软删除默认开启，真实删除仅限归档任务。
- 必建索引场景：
  - 高频筛选字段：`family_id`、`child_id`、`date`
  - 提醒查询：`event_time`、`status`
  - 记账统计：`expense_date`、`category`
  - 地理推荐：`lat/lng` 或 geohash 辅助索引
- Migration 强制管理：禁止直接改线上表结构。
- N+1 查询必须在 Code Review 阶段阻断。

---

## 9. 业务规则落地要求（结合 PRD）

- 吃法推荐：必须过滤过敏原，且每道菜至少 1 个替代方案。
- 穿搭推荐：温度与降水规则参数化，阈值可配置化（不可硬编码）。
- 附近推荐：按年龄阶段配置半径权重，空气质量影响户外权重。
- 日程提醒：支持 `7/3/1/0` 天提醒；同事件同日最多 2 条通知。
- 健康记录：体温 >= 38.0 自动提示；连续异常波动触发复查建议。
- 记账：当月预算达到 90% 时触发预警事件。
- 语音：`parse` 与 `execute` 分离；执行类意图必须有确认机制。

---

## 10. 异常处理与错误码规范

- 统一全局异常过滤器，禁止直接把原始 Error 返回前端。
- 错误码分段建议：
  - `1xxx` 参数错误
  - `2xxx` 鉴权与权限错误
  - `3xxx` 业务规则错误
  - `4xxx` 外部依赖错误
  - `5xxx` 系统内部错误
- 外部依赖失败必须做降级：
  - 天气接口失败：返回最近缓存 + 提示“数据可能延迟”
  - 地图接口失败：返回空列表而非 500
  - 语音服务失败：保留文本输入兜底链路

---

## 11. 日志、监控与可观测性

- 日志必须结构化 JSON，字段最少包含：`timestamp`、`level`、`message`、`requestId`、`userId`、`familyId`、`module`。
- 严禁打印敏感信息：手机号、身份证、精确地址、原始语音内容。
- 指标监控必须覆盖：
  - 接口耗时 P50/P95/P99
  - 错误率（按模块）
  - 第三方依赖成功率
  - 队列堆积长度
  - 定时任务成功率
- 告警策略：
  - 核心链路可用性 < 99.9% 触发 P1
  - 提醒任务延迟 > 5 分钟触发 P1
  - 语音识别成功率突降触发 P2

---

## 12. 安全与隐私合规

- 传输全链路 HTTPS。
- 敏感数据加密存储（如健康备注、联系方式等）。
- 文件上传做类型白名单与病毒扫描。
- 原始语音默认不落库，仅保留结构化意图日志与必要审计字段。
- 学校/政策数据必须记录来源、更新时间与抓取任务 ID。
- 数据导出（CSV）必须鉴权、限频、审计。

---

## 13. 测试规范与质量门禁

- 单元测试覆盖应用层核心规则，覆盖率最低：
  - `lines >= 80%`
  - `branches >= 70%`
- 集成测试覆盖数据库读写、事务、索引命中关键场景。
- e2e 测试至少覆盖：
  - 登录与鉴权
  - 今日看板聚合
  - 提醒创建与触发
  - 记账与月度汇总
  - 语音 parse + execute 主链路
- 每个 PR 必须通过：`lint + test + build + e2e(smoke)`。
- 禁止跳过失败测试直接合并。

---

## 14. 配置与环境管理

- 环境变量统一由 `@nestjs/config` 管理，并使用 schema 校验（Joi/Zod）。
- 环境分层：`local/dev/staging/prod`，配置隔离。
- 禁止将密钥写入代码仓库，统一使用 Secret 管理系统。
- 配置变更需记录变更单与回滚方案。

---

## 15. CI/CD 与发布规范

- 分支模型：`main`（稳定） + `feature/*` + `release/*` + `hotfix/*`。
- Commit 规范：`feat/fix/refactor/test/chore/docs`。
- 发布流程：
  - 自动化构建镜像
  - 自动运行质量门禁
  - staging 冒烟通过后再发布 prod
- 数据库变更必须“先扩展后切换再清理”（兼容式发布）。
- 任何生产故障必须有 24 小时内 RCA（根因分析）报告。

---

## 16. Code Review 审核清单（必须项）

- 是否存在越权风险（`familyId` 作用域）  
- 是否违反 PRD 业务规则（过敏过滤、提醒频控、预算预警等）  
- 是否有性能风险（N+1、全表扫描、大对象序列化）  
- 是否补齐测试（正常流、边界流、异常流）  
- 是否满足可观测性（日志、指标、错误码）  
- 是否包含迁移与回滚方案（涉及 DB 变更时）

---

## 17. MVP 阶段实施优先级建议

- 第一优先：`dashboard + schedule + expense + auth`（形成“看板-提醒-记账”闭环）
- 第二优先：`feeding + nearby(places)`（提升日活与推荐价值）
- 第三优先：`health + voice(query)`（增强记录闭环与输入效率）
- 第四优先：`outfit + schools + voice(execute)`（完善中后期差异化能力）

---

## 18. 附：建议统一的业务错误码示例

```text
3001 CHILD_AGE_STAGE_NOT_MATCH
3002 ALLERGEN_CONFLICT
3003 SCHEDULE_REMIND_LIMIT_REACHED
3004 EXPENSE_BUDGET_NEAR_LIMIT
3005 VOICE_INTENT_NOT_SUPPORTED
3006 VOICE_ACTION_NEED_CONFIRMATION
4001 WEATHER_SERVICE_UNAVAILABLE
4002 MAP_SERVICE_UNAVAILABLE
```
