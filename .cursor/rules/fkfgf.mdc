---
alwaysApply: true
---

# 养娃管家前端开发规范（React + Vite）

> 适用范围：`养娃管家` 小程序/前端应用（PRD v1.0）  
> 技术主栈：`React + Vite + TypeScript`（你写的 `renact` 这里按 `React` 规范输出）  
> 目标：保证前端在 **可维护性、性能、稳定性、可扩展性、可测试性** 上可持续演进。

---

## 1. 规范目标与设计原则

- **业务正确性优先**：先保证分龄推荐、提醒、记账、语音等核心链路正确，再做体验增强。
- **一致性优先**：同类页面交互模式一致（筛选、分页、加载、空态、错误态）。
- **低心智负担**：组件职责单一，目录结构稳定，命名统一，降低协作成本。
- **可观测优先**：关键行为与异常必须可追踪（埋点、错误日志、请求链路）。
- **渐进增强**：MVP先覆盖“看板 + 提醒 + 记账 + 推荐”，复杂功能（语音执行、学校对比）分阶段上线。

---

## 2. 技术栈与版本基线

- 框架：`React 18+`
- 构建：`Vite 5+`
- 语言：`TypeScript`（`strict: true`）
- 路由：`react-router-dom 6+`
- 状态管理：`Zustand`（全局轻量状态）+ `React Query`（服务端状态）
- UI 组件：建议 `TDesign React` 或统一自研组件库
- 样式方案：`CSS Modules` 或 `Tailwind`（二选一，项目内保持唯一）
- 表单：`react-hook-form + zod`
- 图表：`ECharts`（健康/花销趋势）
- 测试：`Vitest + Testing Library + Playwright`
- 质量工具：`ESLint + Prettier + Stylelint + Husky + lint-staged`

---

## 3. 目录结构规范（必须）

```text
src/
  app/                    # 应用初始化（路由、provider、全局配置）
  pages/                  # 页面级组件（按业务域分组）
    dashboard/
    feeding/
    recommend/
    schedule/
    health/
    expense/
    voice/
    profile/
  modules/                # 业务模块（复用逻辑 + 业务组件）
  components/             # 通用组件（无业务语义）
  services/               # API 请求、适配层、DTO 转换
  store/                  # 全局状态（Zustand）
  hooks/                  # 通用 hooks（useDebounce/usePermission 等）
  utils/                  # 纯函数工具
  constants/              # 常量、枚举、错误码映射
  styles/                 # 主题、全局样式变量
  assets/                 # 静态资源
  types/                  # 全局类型声明
  tests/                  # 测试工具与公共 mock
```

- 页面文件命名：`index.tsx` + 同目录 `styles.module.css`。
- 组件文件命名：`PascalCase.tsx`。
- hooks 命名：`useXxx.ts`。
- 禁止跨层直接引用（`pages` 不直接依赖其他页面实现细节）。

---

## 4. TypeScript 与编码规范

- 必须开启严格模式：`noImplicitAny`、`strictNullChecks`。
- 禁止使用 `any`；确实需要时仅允许 `unknown` + 类型收窄。
- 所有接口类型统一在 `services/types.ts` 或模块 `types.ts` 中定义。
- 业务枚举统一管理：年龄段、提醒状态、记账类目、语音意图。
- 函数必须有明确返回类型（特别是 hooks、service 层、selector）。
- 单文件建议不超过 300 行；超出需拆分。
- 复杂逻辑添加“为什么”的注释，避免“是什么”的废话注释。

---

## 5. 组件分层规范

- **页面组件（Page）**：负责页面布局、路由参数、数据拼装。
- **业务组件（Module Component）**：封装领域能力（如预算预警卡片、提醒时间轴）。
- **通用组件（UI Component）**：纯展示，不含业务依赖（Button、Empty、StatCard）。
- **禁止反模式**：
  - 在通用组件内写接口请求
  - 在页面组件中堆积复杂业务计算
  - props 透传超过 3 层（应使用 context/store）

---

## 6. 状态管理与数据流规范

- 服务端状态统一由 `React Query` 管理（缓存、重试、失效、并发控制）。
- 客户端全局状态（用户、家庭信息、筛选条件）使用 `Zustand`。
- 表单状态由 `react-hook-form` 管理，不进入全局 store。
- Query Key 统一命名：`['module', 'resource', params]`。
- 关键写操作（记账、提醒创建、语音执行）必须做防重复提交（按钮锁 + 幂等键）。
- 避免“接口数据直出 UI”，必须经过 `adapter/mapper` 做字段标准化。

---

## 7. API 交互与错误处理规范

- 封装统一请求层：鉴权头、超时、重试、错误码映射、日志上报。
- 统一响应结构适配：`{ code, message, data, requestId }`。
- 错误分层处理：
  - `4xx`：用户可修复（提示可操作文案）
  - `5xx`：系统错误（统一兜底提示 + 上报）
  - 网络异常：弱网提示 + 重试入口
- 所有关键请求需传递并记录 `requestId`，便于前后端联调定位。
- 分页接口统一参数：`page/pageSize/sortBy/sortOrder`，禁止自定义命名漂移。

---

## 8. PRD 关键业务前端实现约束

### 8.1 首页看板（Dashboard）
- 首屏目标：4G 网络下关键卡片可见 <= 2s。
- 卡片按优先级渲染：提醒 > 今日建议 > 花销速览 > 推荐内容。
- 定位/天气未授权时展示可操作授权引导，不显示空白卡片。

### 8.2 分龄吃法
- 必须展示年龄段来源与过敏过滤结果。
- 每道推荐菜品显示至少一个替代方案入口。
- 推荐失败时提供“默认健康模板”兜底。

### 8.3 穿搭对比
- 最多 3 品牌并列，超出必须阻止并提示。
- 温度/降水规则要可视化（为什么推荐该搭配）。

### 8.4 附近推荐（玩乐/学校）
- 地图与列表支持一键切换，状态保持一致（筛选、排序、选中项）。
- 低龄默认距离优先，天气差自动提升室内权重标签可见。

### 8.5 日程提醒
- 提醒创建流程必须可回看、可编辑、可标记完成。
- 逾期事件单独分组展示，避免和未来事件混杂。

### 8.6 健康与花销报表
- 图表必须支持时间维度切换（周/月/季度）。
- 超预算 90% 需高优先级视觉告警，不可仅 toast。

### 8.7 语音功能
- `parse` 与 `execute` 在交互上分离展示（先理解、再执行）。
- 删除/批量修改类操作必须二次确认弹窗。
- 语音失败时可无缝回退为文本输入。

---

## 9. UI/UX 与交互规范

- 设计令牌统一：颜色、间距、圆角、阴影、字号，不允许页面私有魔法值。
- 触控目标最小尺寸 >= 44px。
- 统一状态组件：`Loading`、`Empty`、`Error`、`NoPermission`。
- Toast 文案规则：短句 + 可执行建议（例如“网络异常，请重试”）。
- 对比/报表类信息要“先结论后细节”，降低家长阅读成本。
- 高风险操作按钮（删除、清空）必须二次确认并区分色。

---

## 10. 性能规范

- 路由与大模块必须懒加载：`React.lazy + Suspense`。
- 图片资源启用压缩与尺寸分级，非首屏资源延迟加载。
- 长列表（推荐、学校、账单）使用虚拟滚动或分页分段渲染。
- 避免无意义 re-render：memo、selector、稳定引用（`useMemo/useCallback`）。
- 首屏关键请求并行化，非关键请求延后（如埋点配置、次级模块）。
- 构建包体积门禁：单入口包超阈值（例如 300KB gzip）必须告警。

---

## 11. 安全与隐私规范

- 禁止在前端持久化敏感隐私明文（健康详情、精确定位等）。
- token 存储遵循平台安全建议，避免暴露到日志与 URL。
- 文件上传前做类型、大小校验；异常回传用户友好提示。
- 埋点数据脱敏：不采集原始语音、身份证号、手机号全量。
- 所有外链跳转需白名单校验（学校政策来源页面）。

---

## 12. 埋点与可观测规范

- 必埋点事件（与 PRD 对齐）：
  - 首页卡片点击
  - 推荐点击/收藏
  - 提醒创建/完成
  - 语音按钮点击/识别成功/意图命中
  - 记账提交成功率
- 埋点字段标准：`eventName`、`userId`、`familyId`、`childStage`、`timestamp`、`traceId`。
- 错误上报需带上下文：路由、接口、设备信息、requestId。
- 关键漏斗日报：看板访问 -> 推荐点击 -> 记账/提醒完成。

---

## 13. 测试规范与质量门禁

- 单元测试：工具函数、hooks、业务组件核心逻辑。
- 组件测试：加载态/空态/错误态/权限态必须覆盖。
- e2e 覆盖主链路：
  - 登录鉴权
  - 今日看板加载
  - 新增提醒并完成
  - 新增记账并查看月汇总
  - 语音 parse 到 execute（含二次确认）
- 覆盖率门槛建议：
  - `lines >= 80%`
  - `branches >= 70%`
- CI 必过：`type-check + lint + unit + e2e-smoke + build`。

---

## 14. Git 与协作规范

- 分支命名：`feature/*`、`fix/*`、`refactor/*`。
- Commit 规范：`feat`、`fix`、`refactor`、`test`、`docs`、`chore`。
- PR 模板必须包含：
  - 变更背景
  - 影响范围
  - 自测清单
  - 回滚方案
  - 截图/录屏（涉及交互变更）
- 禁止超大 PR（建议 < 500 行有效改动），超出需拆分。

---

## 15. CI/CD 与发布规范

- 环境隔离：`dev/staging/prod`，变量由环境注入，不进仓库。
- 每次发布自动生成版本号与 changelog。
- 灰度发布优先覆盖核心人群，关注识别成功率与崩溃率。
- 线上事故处理：10 分钟内止血，24 小时内复盘报告。

---

## 16. 建议落地顺序（MVP 对齐）

- 第一阶段：看板、提醒、记账、登录鉴权
- 第二阶段：吃法推荐、附近玩乐
- 第三阶段：健康曲线、语音查询
- 第四阶段：穿搭对比、学校推荐、语音操作

---

## 17. 代码评审硬性检查项

- 是否遵循目录与分层规范  
- 是否存在业务规则遗漏（过敏过滤、预算预警、提醒频控）  
- 是否处理完整状态（loading/empty/error/no-permission）  
- 是否有性能隐患（重复渲染、超大组件、阻塞首屏）  
- 是否补齐测试与埋点  
- 是否包含可回滚方案
